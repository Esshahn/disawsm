export function get_config() {
  const config = {
    version: "26.01.04",
    default_filename: "mycode",
    window_editor: {
      top: 50,
      left: 210,
      autoOpen: true,
      closeable: false,
    },
  };

  return config;
}
